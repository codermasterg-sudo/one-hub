╔════════════════════════════════════════════════════════╗
║          One Hub 生产环境部署文件生成完成               ║
╚════════════════════════════════════════════════════════╝

已生成以下部署文件：

📁 核心配置文件
├── Makefile.prod                  ⭐ 生产环境 Make 管理工具
├── docker-compose.prod.yml        ⭐ 生产环境 Docker Compose 配置
└── .env.production.example        ⭐ 环境变量配置模板

📁 脚本工具
├── scripts/backup.sh              📦 数据库备份脚本
├── scripts/restore.sh             📦 数据库恢复脚本
└── scripts/quick-start.sh         🚀 一键部署脚本

📁 文档
├── DEPLOY.md                      📖 详细部署文档
└── PRODUCTION.md                  📖 生产环境总文档

════════════════════════════════════════════════════════

🚀 快速开始 - 三种部署方式

方式一：一键部署（最简单）
─────────────────────────────
bash scripts/quick-start.sh

方式二：使用 Make 部署（推荐）
─────────────────────────────
make -f Makefile.prod init
make -f Makefile.prod start

方式三：手动部署（完全控制）
─────────────────────────────
cp .env.production.example .env.production
# 编辑配置文件
docker-compose -f docker-compose.prod.yml up -d

════════════════════════════════════════════════════════

📋 常用命令速查

服务管理：
  make -f Makefile.prod help      # 查看所有命令
  make -f Makefile.prod start     # 启动服务
  make -f Makefile.prod stop      # 停止服务
  make -f Makefile.prod restart   # 重启服务
  make -f Makefile.prod status    # 查看状态

日志查看：
  make -f Makefile.prod logs      # 查看所有日志
  make -f Makefile.prod logs-app  # 查看应用日志

数据库：
  make -f Makefile.prod db-backup # 备份数据库
  make -f Makefile.prod db-shell  # 进入 MySQL

更新：
  make -f Makefile.prod update    # 更新到最新版本

════════════════════════════════════════════════════════

⚠️ 重要提示

1. 首次部署后请立即修改默认密码（root/123456）
2. 生产环境强烈建议配置 HTTPS 反向代理
3. 设置定时备份：
   crontab -e
   0 2 * * * cd /path/to/one-hub && make -f Makefile.prod db-backup

4. USER_TOKEN_SECRET 一旦设置后不可修改！
5. 定期更新系统和 Docker 镜像

════════════════════════════════════════════════════════

📞 获取帮助

详细文档：  DEPLOY.md 或 PRODUCTION.md
项目文档：  https://one-hub-doc.vercel.app/
GitHub：    https://github.com/MartialBE/one-hub
问题反馈：  https://github.com/MartialBE/one-hub/issues

════════════════════════════════════════════════════════

祝部署顺利！🎉

